'use strict';

const Alexa = require('alexa-sdk');
const questions = require('./question');

const APP_ID = 'amzn1.ask.skill.'; //could not generate id because of slot types

exports.handler = function (event, context) {  
};
